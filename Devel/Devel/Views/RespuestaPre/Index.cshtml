@model IEnumerable<Devel.Models.RespuestaPre>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdResEnc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdPreg)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Respuesta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdOpcion)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdResEnc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPreg)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Respuesta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdOpcion)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.IdResPre }) |
                @Html.ActionLink("Details", "Details", new { id = item.IdResPre }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.IdResPre })
            </td>
        </tr>
    }

</table>
